<div class = "jumbotron text-center">
    <h1>Welcome to Sketch Max</h1>
    <h3>5 Dollar Sketches, every month!</h3>
</div>
<div class="row">
    <% if user_signed_in? %>
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Your Profile</h2>
                <% if current_user.profile %>
                    <%= link_to "Edit profile", "#", class: "btn btn-default btn-lg btn-block" %><br/>
                    <%= link_to "View Profile", user_path(current_user), class: "btn btn-default btn-lg btn-block" %>
                <% else %>
                    <p>Create your profile so that you can share your information with the community.</p>
                    <%= link_to "Create Profile", new_user_profile_path(current_user), class: "btn btn-default btn-lg btn-block" %>
                <% end %>
            </div>
        </div>
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">The Community</h2>
                <p>Visit the community to meet other artists, collectors, and curators!</p>
                <%= link_to "Visit the community", "#", class: "btn btn-default btn-lg btn-block" %>
            </div>
        </div>
    <% else %>
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Basic Membership</h2>
                <h4>Sign up for free and get basic access to the community and show off your art skills!</h4>
                <%= link_to "Sign up for Basic", new_user_registration_path(plan: @basic_plan.id), class: "btn btn-primary btn-lg btn-block" %>
            </div>
        </div>
        <div class="col-md-6">
            <div class="well">
                <h2 class="text-center">Primo Membership</h2>
                <h4>Sign up for the primo account for just $5/month and be paired with new artists every month!</h4>
                <%= link_to "Sign up for Primo", new_user_registration_path(plan: @pro_plan.id), class: "btn btn-success btn-lg btn-block" %>
            </div>
        </div>
    <% end %>
</div>